Algoritmo "Exercicio 2"

Var
   texto, char: caracter
   indice, soma, tam: inteiro

Inicio

   escreval ("Digite um texto para inverter")
   leia (texto)

   tam <- compr (texto)

   para indice de tam ate 1 passo -1 faca
      char <- Copia(texto, indice, 1)
      escreva(char)
   fimpara




Fimalgoritmo