Algoritmo "Exercicio 1"

Var
texto, char: caracter
indice, soma, tam: inteiro

Inicio

escreval ("Digite um texto para a soma:")
leia (texto)

tam <- compr (texto)

para indice de 1 ate tam faca
char <- Copia(texto, indice, 1)
soma <- soma + asc(char)
fimpara

escreval("resultado:",soma)


Fimalgoritmo